---
- name: Bootstrap Hyperviser
  hosts: hyperviser
  become: true
  become_method: su
  become_user: root
  
  vars:
    ansible_user: "{{ bootstrap_user }}"
    ansible_password: "admin2010"

  roles:
    - role: bootstrap_debian-linux
