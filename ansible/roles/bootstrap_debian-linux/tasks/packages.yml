---
- name: Remove CDROM Packages
  lineinfile:
    dest: "/etc/apt/sources.list"
    regexp: "^deb cdrom:"
    line: ""

- name: Update
  apt:
    update_cache: yes
    upgrade: yes
  ignore_errors: true

- name: Install Basic Apps
  apt:
    name: "{{ basic_apps }}"
